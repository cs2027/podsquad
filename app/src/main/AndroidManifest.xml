<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.song.christopher.podsquad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.song.christopher.podsquad.LoginActivityScreens.NewGroupActivity"></activity>
        <activity android:name="com.song.christopher.podsquad.GroupActivityScreens.EditTaskActivity" />
        <activity android:name="com.song.christopher.podsquad.GroupActivityScreens.TaskActivity" />
        <activity android:name="com.song.christopher.podsquad.GroupActivityScreens.GroupActivity" />
        <activity android:name="com.song.christopher.podsquad.LoginActivityScreens.WelcomeActivity" />
        <activity android:name="com.song.christopher.podsquad.LoginActivityScreens.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>